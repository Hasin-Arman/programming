#include<stdio.h>

int main()
{
    int a,b,c,d,i,lar,small;
    scanf("%d%d%d",&a,&b,&c);

  if(a>1 && b>1 && c>1){
    printf("chayan");
  }
    if(a>1 && b>1 && c>1){
    printf("Arman");
  }

     return 0;
}
